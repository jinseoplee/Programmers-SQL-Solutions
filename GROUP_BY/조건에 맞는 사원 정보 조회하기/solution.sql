SELECT
    SUM(B.SCORE) AS SCORE,
    A.EMP_NO,
    A.EMP_NAME,
    A.POSITION,
    A.EMAIL
FROM
    HR_EMPLOYEES AS A
JOIN
    HR_GRADE AS B ON A.EMP_NO = B.EMP_NO
WHERE 
    YEAR = '2022'
GROUP BY 
    B.EMP_NO
ORDER BY
    SCORE DESC
LIMIT
    1